package com.sygneto.domain;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;
import com.sygneto.web.rest.TestUtil;

public class ItemQuotationTest {

   /* @Test
    public void equalsVerifier() throws Exception {
        TestUtil.equalsVerifier(ItemQuotation.class);
        ItemQuotation itemQuotation1 = new ItemQuotation();
        itemQuotation1.setId(1L);
        ItemQuotation itemQuotation2 = new ItemQuotation();
        itemQuotation2.setId(itemQuotation1.getId());
        assertThat(itemQuotation1).isEqualTo(itemQuotation2);
        itemQuotation2.setId(2L);
        assertThat(itemQuotation1).isNotEqualTo(itemQuotation2);
        itemQuotation1.setId(null);
        assertThat(itemQuotation1).isNotEqualTo(itemQuotation2);
    }*/
}
